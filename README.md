# Warehouse Assistant (ТОС + AI)

Монорепозиторій MVP для оптимізації складу за принципами ТОС із AI-асистентом.

## Структура
- `backend/` — Express + TypeScript API, Prisma ORM, OpenAI інтеграція.
- `frontend/` — Next.js 14 (App Router) з React Query, TanStack Table.
- `infra/` — docker-compose, діаграми та інфраструктурні артефакти.
- `docs/` — план реалізації та додаткові специфікації.

## Швидкий старт (WIP)
```bash
npm install
cp backend/.env.example backend/.env
cp frontend/.env.example frontend/.env
npm run dev:backend
npm run dev:frontend
```

- Реєстрація нової організації: `POST /api/auth/register` (поля `org_name`, `email`, `password`, опціонально `warehouse_name`, `name`). У відповіді повертається JWT, тож одразу можна працювати.

## Дані та БД
1. Підійміть Postgres/Redis локально (або `docker compose up -d db redis` з `infra/docker-compose.yml`).
2. Застосуйте міграції: `npm --workspace backend run db:deploy` (перший раз `db:push` також працює).
3. Наповніть тестовими даними: `npm --workspace backend run seed`.
   - Створюється організація `demo-org` з 4 складами (`WH-KYIV`, `WH-LVIV`, `WH-KHARKIV`, `WH-ODESA`).
   - Каталог: 100 SKU запчастин із сегментами A/B/C, сезонністю та змінним попитом; продажі за ~6 місяців, денні залишки (100 днів) і реалістичні закупівлі.
   - Після сіду автоматично відновлюється `sales_daily`, перераховуються ТОС-буфери й формується статистика lead time.
   - Дані для входу: `demo@warehouse.ai` / `Demo1234!` (`POST /api/auth/login`).
4. Тестові запити (оберіть склад `WH-KYIV/WH-LVIV/WH-KHARKIV/WH-ODESA` та SKU `SKU-001…SKU-100`):
   - `GET /api/recommendations?org_id=demo-org&warehouse_id=WH-KYIV&page=1&page_size=50`.
   - `GET /api/buffers?org_id=demo-org&warehouse_id=WH-KHARKIV&recalc=true`.
   - `GET /api/kpi/sku/SKU-010?org_id=demo-org&warehouse_id=WH-ODESA`.

## Імпорт даних з 1С (DBF)
1. Розпакуйте файл, наприклад `ОборотыTEST.dbf`.
2. Запустіть скрипт:
   ```bash
   npm --workspace backend run import:turnover -- --file ../ОборотыTEST.dbf --org demo-org --limit 200000
   ```
   де `--limit` опційний (для швидкого прогону на частині записів).
3. Скрипт:
   - автоматично створює склади (`warehouses`) за назвою з DBF (ID = `WH-<md5>`),
   - додає SKU в `catalog` (ID = DBF `ID`, назва = `TOVAR`),
   - записує продажі (`PRODANO`) у `sales_events` та знімки запасів (`OSTATOK`) у `stock_snapshots`,
   - після імпорту перебудовує `sales_daily` і перераховує буфери для кожного складу.

Відповідність полів:

| DBF поле | Призначення | Наші таблиці |
| --- | --- | --- |
| `DATE` (YYYYMMDD) | Дата продажу/змін | `sales_events.order_datetime`, `stock_snapshots.date`, `sales_daily.date` |
| `SKLAD` | Назва складу | `warehouses.name` (ID = `WH-<md5>` від назви) |
| `ID` | Унікальний SKU (UUID) | `catalog.sku` |
| `TOVAR` | Назва товару | `catalog.name` |
| `PRODANO` | Кількість проданого | `sales_events.qty` (канал `1C`), далі потрапляє в `sales_daily.units` |
| `OSTATOK` | Залишок на дату | `stock_snapshots.qty_on_hand` |

## API-інтеграції
- Отримайте `API key` у веб-інтерфейсі (сторінка “Налаштування”) або через `GET /api/auth/api-key`.
- Усі інтеграційні запити до `/api/ingest/*` мають містити заголовок `X-API-Key: <Ваш ключ>`. Це дозволяє підключати 1С/CRM/складські системи без JWT.
- Payload-и відповідають опису з `docs/integration-spec.md`. Після відправки даних ідемпотентний upsert оновлює записи у вашій організації.
- Список складів, що доступні користувачу: `GET /api/warehouses` (авторизація обов’язкова). Використовується фронтендом для побудови селекторів і пагінації рекомендацій.

### Приклади API

**Отримати склади**
```
GET /api/warehouses?search=Київ
Authorization: Bearer <JWT>

Response: {
  "data": [
    {
      "id": "WH-6DEB3C1E",
      "name": "Київський хаб",
      "latestStockDate": "2025-09-30T00:00:00.000Z"
    },
    ...
  ]
}
```

**Отримати рекомендації (пагіновано)**
```
GET /api/recommendations?org_id=demo-org&warehouse_id=WH-6DEB3C1E&page=1&page_size=50&date=2025-11-01
Authorization: Bearer <JWT>

Response:
{
  "org_id": "demo-org",
  "warehouse_id": "WH-6DEB3C1E",
  "date": "2025-11-01",
  "effective_date": "2025-09-30",
  "page": 1,
  "page_size": 50,
  "total": 1342,
  "data": [
    {
      "sku": "09366e8f-...",
      "category": "HEETS",
      "segment": "A",
      "zone": "red",
      "target": 420,
      "onHand": 120,
      "monthlyDemand": 450,
      "suggestedQty": 300,
      "overstock": null,
      "avgDailyDemand": 15,
      "leadTimeDays": 7,
      "daysOfSupply": 8,
      "bufferPenetration": 0.28,
      ...
    }
  ]
}

> 🔎 `effective_date` показує фактичний день, за який вдалося зняти залишки. Якщо ви запитуєте майбутню дату, а у сховищі ще немає знімка, бекенд автоматично віддає останні доступні запаси (і фронт показує позначку “останній доступний знімок”).
>
> ⚠️ `page_size` обмежений 200, тож для масових сценаріїв використовуйте пагінацію або кілька паралельних запитів. Фронтенд не перевищує цей ліміт (20/50/100/150/200).
```

Фронтенд використовує ці ж ендпоінти:
- `GET /warehouses` → селектор складів у UI (якщо жодного складу немає, сторінка показує підказку, що потрібно імпортувати дані).
- `GET /recommendations` з `page/page_size` → таблиця з пагінацією (20/50/100/150), пошуком і фільтрами (категорія/сегмент/зона/overstock). На кожному рядку бачимо і SKU, і назву товару, місячний попит, сегмент, DoS та перезатарювання (якщо є).
- У блоці “Як читати таблицю” на фронті описані всі формули ТОС: `zone = (on_hand + inbound - reservations) / buffer_qty`, `target = avg_daily_demand × lead_time_days × buffer_factor`, `stock_position = on_hand + inbound - reservations`, `Suggested = ceil_to_pack(max(0, target - stock_position))`, `Overstock`, `DoS`, `Penetration` тощо. Кожен показник підписаний джерелом даних (`catalog`, `stock_snapshots`, `sales_daily`, `purchase_orders`, таблиці буферів), тому користувачі бачать той самий опис, що й у цій документації.
- Мережевий блок під таблицею показує картки всіх складів (on hand, suggested, кількість червоних/overstock SKU) і рекомендацію, що робити зі складом; клік по картці переводить до детального перегляду цього складу.
- При введенні тексту в полі “Пошук за SKU або поясненням” запускається комбінований пошук по всій мережі: окремий блок “Пошук SKU по мережі” показує, на яких складах знайдено SKU/назву/причину, і підсумовує on hand/inbound/target/suggested між майданчиками. Це дозволяє миттєво побачити повний розподіл конкретного SKU без перемикання складів.

## Як працює ТОС + AI
- **Детерміноване ядро ТОС**:
  - `avg_daily_demand` — середній денний попит із `sales_daily` (останні 60 днів).
  - `lead_time_days` — медіанний час між `purchase_orders.ordered_at` і `received_at` (фолбек — 7 днів).
  - `buffer_factor` — коеф. безпеки (1.2 за замовчуванням; AI може рекомендувати інше).
  - Ціль буфера: `target = avg_daily_demand × lead_time_days × buffer_factor`. Зона визначається за `stock_position / buffer_qty`. `order_qty = ceil_to_pack(max(0, buffer_qty - stock_position))`.
- **AI рівень**: асистент через function calling викликає `get_top_skus`, `get_sales_by_day`, `get_stock_status`, `get_buffer_status`, `get_purchase_orders`, `get_recommendations_for_sku` і формує відповідь на реальних даних. Рекомендації також сигналізують про перезатарювання (якщо фактичний запас > ~місячного попиту), щоб коригувати буфер або планувати промо.
- **Нові AI-інструменти**:
  - `get_stock_by_warehouse` — показує розподіл залишків конкретного SKU між складами (обрана дата або останній знімок).
  - `get_sales_summary` — агрегує продажі за довільний період і групує їх за складами/sku для порівнянь попиту.
  - `suggest_rebalance` — шукає склади з надлишком/дефіцитом і пропонує, що та скільки можна перемістити між майданчиками.
- **Історія/сезонність**: `sales_daily` зберігає 60–180 днів, builder оновлює ряди щодня; `lead_time_stats` накопичує LT по постачальниках. Це дозволяє AI бачити тренди, а ядру ТОС — працювати детерміновано.

## Дорожня карта
Детальний план знаходиться у `docs/project-plan.md`. Фази:
1. Архітектура й сетап
2. Дані та ingest-сервіси
3. Агрегації і ТОС-розрахунки
4. REST API + Swagger
5. Frontend MVP
6. AI-асистент
7. Docker/Ops

## Стек
Node.js 20 · Express · Prisma ORM · PostgreSQL · Redis · BullMQ · OpenAI API · Next.js 14 · React Query · TanStack Table · Docker

## Статус реалізації
**Готово зараз**
- Автентифікація (реєстрація `POST /api/auth/register`, логін, мульти-організаційний JWT).
- Інгест-API для catalog, warehouses, suppliers, sales, stock, PO header/lines (idempotent upsert).
- ТОС-ядро: `sales_daily` builder, буфери, зони, DoS/Turns/FEFO, рекомендації, KPI та сигналізація про перезатарювання.
- AI-шар: `/assistant/query`, `/assistant/explain`, function calling (`get_top_skus`, `get_sales_by_day`, `get_stock_status`, `get_buffer_status`, `get_purchase_orders`, `get_recommendations_for_sku`).
- Frontend (Next.js 14): рекомендації з клієнтськими фільтрами (пошук, категорії, сегменти, зона, overstock, “Всі склади”), KPI, імпорт, AI-чат, login/register.
- Документація інтеграцій (`docs/integration-spec.md`) — опис CSV-потоків.

**Ще потрібно**
- Підключити реальні вивантаження (SFTP/webhook) і автоматизувати ingest pipeline.
- Розширити аналітику (ABC/XYZ, промо-факти, бюджетні обмеження) після появи живих даних.
- UI-додатки: дашборди по буферам, експорт у CSV/Excel.
- Ops: моніторинг, алерти, повторні спроби обміну та узгодження SLA з 1С.
